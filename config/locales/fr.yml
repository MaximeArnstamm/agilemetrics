fr:
  date:
    formats:
      default:
        "%d/%m/%Y"
      with_weekday:
        "%a %d/%m/%y"

  time:
    formats:
      default:
        "%a, %b %-d, %Y at %r"
      date:
        "%b %-d, %Y"
      short:
        "%B %d"

  titles:
    application: Agilemetrics

  activerecord:
        errors:
            models:
                sprint:
                  blank: "Champs obligatoire"
